spring:
  # DB
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ex04?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: black!0501
    
  #  data,sql ?? ??
  sql:
    init:
      # always, never, embedded, embedded-always
      mode: never

  #  jpa
  jpa:
    show-sql: true
    hibernate:
      # create create-drop update none
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    #    sql ?? ?? ??
    defer-datasource-initialization: false

# swagger
springdoc:
  api-docs:
    enabled: true
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  swagger-ui:
    operations-sorter: alpha
    tags-sorter: alpha
    path: /swagger-ui  # ✅ 기본 경로 유지
    enabled: true
  packages-to-scan: com.example.kdh
  paths-to-match:
    - /api/**/**

# jwt
token:
  key: 571d9f6ba03cb0051b3f5f1cfdffa1fd
